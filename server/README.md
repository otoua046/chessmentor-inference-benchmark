# Server Module

Pipeline B uses Roboflow Hosted API.

This folder optionally contains a thin FastAPI wrapper
if direct calls from the mobile app are not used.

Ensure model version and thresholds match on-device inference.
